//square root function sqrt();
#include <stdio.h>
#include <math.h>
int main()
{
	int num;
	printf ("Enter a number ");
	scanf ("%d",&num);
	printf ("The square root is %.2f",sqrt(num));	
	return 0;
}
